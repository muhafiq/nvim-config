require("toggleterm").setup({
  -- default direction, bisa diubah-ubah nanti per terminal instance
  direction = "horizontal",
  open_mapping = [[<C-\>]],
  shade_terminals = true,
  insert_mappings = true,
  start_in_insert = true,
  persist_mode = false,
})

